src/.env
.env
.vscode
.idea
/src/composer.lock
/src/vendor
/src/Application/application.json
docker-compose.override.yml
.DS_Store

# Dev tooling local overrides
/src/phpstan.neon
/src/phpunit.xml
/src/.phpunit.cache
/src/.phpunit.result.cache

# MkDocs build output
/site

# Drop-in SPA build target — CI / dev másolja ide a `dist/`-et.
# A mappa maga (.gitkeep-pel) verzionált, a tartalma nem.
/src/html/app/*
!/src/html/app/.gitkeep

# Var (runtime artefactek): JWT kulcsok, route cache, log fájlok
/src/var/
